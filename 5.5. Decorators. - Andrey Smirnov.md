# 5.5. # Домашнее задание к лекции «Decorators». - Андрей Смирнов.

1. Доработать декоратор `logger` в коде ниже. Должен получиться декоратор, который записывает в файл 'main.log'  дату и время вызова функции, имя функции, аргументы, с которыми вызвалась, и возвращаемое значение. Функция `test_1` в коде ниже также должна отработать без ошибок.

```python
import os


def logger(old_function):
    ...

    def new_function(*args, **kwargs):
        ...

    return new_function


def test_1():

    path = 'main.log'
    if os.path.exists(path):
        os.remove(path)

    @logger
    def hello_world():
        return 'Hello World'

    @logger
    def summator(a, b=0):
        return a + b

    @logger
    def div(a, b):
        return a / b

    assert 'Hello World' == hello_world(), "Функция возвращает 'Hello World'"
    result = summator(2, 2)
    assert isinstance(result, int), 'Должно вернуться целое число'
    assert result == 4, '2 + 2 = 4'
    result = div(6, 2)
    assert result == 3, '6 / 2 = 3'
    
    assert os.path.exists(path), 'файл main.log должен существовать'

    summator(4.3, b=2.2)
    summator(a=0, b=0)

    with open(path) as log_file:
        log_file_content = log_file.read()

    assert 'summator' in log_file_content, 'должно записаться имя функции'
    for item in (4.3, 2.2, 6.5):
        assert str(item) in log_file_content, f'{item} должен быть записан в файл'


if __name__ == '__main__':
    test_1()

```


------


### Ответ:


Листинг кода, лога и сами файлы [5-5_z1.py](./assets/5_5/5-5_z1.py) и [main.log](./assets/5_5/main_z1.log):

```python
import os
from datetime import datetime

def logger(old_function):
    def new_function(*args, **kwargs):
        # Формируем строку аргументов
        args_str = ', '.join(repr(arg) for arg in args)
        kwargs_str = ', '.join(f'{k}={repr(v)}' for k, v in kwargs.items())
        all_args = ', '.join(filter(None, [args_str, kwargs_str]))

        # Вызываем функцию
        result = old_function(*args, **kwargs)

        # Записываем лог
        with open('main.log', 'a', encoding='utf-8') as log_file:
            log_file.write(
                f'{datetime.now()} – {old_function.__name__}({all_args}) -> {repr(result)}\n'
            )

        return result

    return new_function


def test_1():
    path = 'main.log'
    if os.path.exists(path):
        os.remove(path)

    @logger
    def hello_world():
        return 'Hello World'

    @logger
    def summator(a, b=0):
        return a + b

    @logger
    def div(a, b):
        return a / b

    assert 'Hello World' == hello_world(), "Функция возвращает 'Hello World'"
    result = summator(2, 2)
    assert isinstance(result, int), 'Должно вернуться целое число'
    assert result == 4, '2 + 2 = 4'
    result = div(6, 2)
    assert result == 3, '6 / 2 = 3'

    assert os.path.exists(path), 'файл main.log должен существовать'

    summator(4.3, b=2.2)
    summator(a=0, b=0)

    with open(path) as log_file:
        log_file_content = log_file.read()

    assert 'summator' in log_file_content, 'должно записаться имя функции'
    for item in (4.3, 2.2, 6.5):
        assert str(item) in log_file_content, f'{item} должен быть записан в файл'


if __name__ == '__main__':
    test_1()
```

Лог:

```
2026-06-24 21:23:26.577475 – hello_world() -> 'Hello World'
2026-06-24 21:23:26.577862 – summator(2, 2) -> 4
2026-06-24 21:23:26.578243 – div(6, 2) -> 3.0
2026-06-24 21:23:26.578544 – summator(4.3, b=2.2) -> 6.5
2026-06-24 21:23:26.578866 – summator(a=0, b=0) -> 0
```



2. Доработать параметризованный декоратор logger в коде ниже. Должен получиться декоратор, который записывает в файл дату и время вызова функции, имя функции, аргументы, с которыми вызвалась, и возвращаемое значение. Путь к файлу должен передаваться в аргументах декоратора. Функция `test_2` в коде ниже также должна отработать без ошибок.

```python
import os


def logger(path):
    ...
    
    def __logger(old_function):
        def new_function(*args, **kwargs):
            ...

        return new_function

    return __logger


def test_2():
    paths = ('log_1.log', 'log_2.log', 'log_3.log')

    for path in paths:
        if os.path.exists(path):
            os.remove(path)

        @logger(path)
        def hello_world():
            return 'Hello World'

        @logger(path)
        def summator(a, b=0):
            return a + b

        @logger(path)
        def div(a, b):
            return a / b

        assert 'Hello World' == hello_world(), "Функция возвращает 'Hello World'"
        result = summator(2, 2)
        assert isinstance(result, int), 'Должно вернуться целое число'
        assert result == 4, '2 + 2 = 4'
        result = div(6, 2)
        assert result == 3, '6 / 2 = 3'
        summator(4.3, b=2.2)

    for path in paths:

        assert os.path.exists(path), f'файл {path} должен существовать'

        with open(path) as log_file:
            log_file_content = log_file.read()

        assert 'summator' in log_file_content, 'должно записаться имя функции'

        for item in (4.3, 2.2, 6.5):
            assert str(item) in log_file_content, f'{item} должен быть записан в файл'


if __name__ == '__main__':
    test_2()

```

------


### Ответ:


Листинг кода, лога и сами файлы [5-5_z2.py](./assets/5_5/5-5_z2.py) и логи [log_1.log](./assets/5_5/log_1.log), [log_2.log](./assets/5_5/log_2.log), [log_3.log](./assets/5_5/log_3.log):

```python
import os
from datetime import datetime

def logger(path):
    def __logger(old_function):
        def new_function(*args, **kwargs):
            args_str = ', '.join(repr(arg) for arg in args)
            kwargs_str = ', '.join(f'{k}={repr(v)}' for k, v in kwargs.items())
            all_args = ', '.join(filter(None, [args_str, kwargs_str]))
            result = old_function(*args, **kwargs)
            with open(path, 'a', encoding='utf-8') as log_file:
                log_file.write(f'{datetime.now()} – {old_function.__name__}({all_args}) -> {repr(result)}\n')
            return result
        return new_function
    return __logger


def test_2():
    paths = ('log_1.log', 'log_2.log', 'log_3.log')

    for path in paths:
        if os.path.exists(path):
            os.remove(path)

        @logger(path)
        def hello_world():
            return 'Hello World'

        @logger(path)
        def summator(a, b=0):
            return a + b

        @logger(path)
        def div(a, b):
            return a / b

        assert 'Hello World' == hello_world(), "Функция возвращает 'Hello World'"
        result = summator(2, 2)
        assert isinstance(result, int), 'Должно вернуться целое число'
        assert result == 4, '2 + 2 = 4'
        result = div(6, 2)
        assert result == 3, '6 / 2 = 3'
        summator(4.3, b=2.2)

    for path in paths:
        assert os.path.exists(path), f'файл {path} должен существовать'
        with open(path) as log_file:
            log_file_content = log_file.read()
        assert 'summator' in log_file_content, 'должно записаться имя функции'
        for item in (4.3, 2.2, 6.5):
            assert str(item) in log_file_content, f'{item} должен быть записан в файл'


if __name__ == '__main__':
    test_2()
```

log1:

```
2026-06-24 21:32:28.998870 – hello_world() -> 'Hello World'
2026-06-24 21:32:28.999347 – summator(2, 2) -> 4
2026-06-24 21:32:28.999610 – div(6, 2) -> 3.0
2026-06-24 21:32:28.999950 – summator(4.3, b=2.2) -> 6.5
```

log2:

```
2026-06-24 21:32:29.000405 – hello_world() -> 'Hello World'
2026-06-24 21:32:29.000777 – summator(2, 2) -> 4
2026-06-24 21:32:29.001111 – div(6, 2) -> 3.0
2026-06-24 21:32:29.001391 – summator(4.3, b=2.2) -> 6.5
```

log3:

```
2026-06-24 21:32:29.001897 – hello_world() -> 'Hello World'
2026-06-24 21:32:29.002360 – summator(2, 2) -> 4
2026-06-24 21:32:29.002620 – div(6, 2) -> 3.0
2026-06-24 21:32:29.002992 – summator(4.3, b=2.2) -> 6.5
```


3. Применить написанный логгер к приложению из любого предыдущего д/з.

------


### Ответ:


Листинг кода (взял задание с форматированием телефонной книги и логгер из 1го задания), лога и сами файлы [5-5_z3.py](./assets/5_5/5-5_z3.py) и [main.log](./assets/5_5/main_z3.log):

```python
import re
import csv
from pprint import pprint
from decorator.logger import logger

with open("phonebook_raw.csv", encoding="utf-8") as f:
    rows = csv.reader(f, delimiter=",")
    contacts_list = list(rows)
pprint(contacts_list)

header = contacts_list[0]
data = contacts_list[1:]

@logger
def normalize_name(row):
    full_name = ' '.join(row[:3]).split()
    while len(full_name) < 3:
        full_name.append('')
    return full_name + row[3:]

normalized = []
for row in data:
    normalized.append(normalize_name(row))

@logger
def format_phone(phone):
    if not phone:
        return ''
    # Извлекаем добавочный номер, учитывая возможные скобки
    ext_match = re.search(r'(?:\(?доб\.?\s*(\d+)\)?)', phone, re.IGNORECASE)
    ext = ext_match.group(1) if ext_match else None
    # Удаляем добавочный номер вместе со скобками и пробелами
    phone_clean = re.sub(r'\(?доб\.?\s*\d+\)?', '', phone, flags=re.IGNORECASE)
    # Удаляем оставшиеся пустые скобки
    phone_clean = re.sub(r'\(\)', '', phone_clean)
    phone_clean = phone_clean.strip()
    # Форматируем основной номер
    pattern = r'(\+7|8)?\s*\(?(\d{3})\)?\s*[-]?(\d{3})[-]?(\d{2})[-]?(\d{2})'
    formatted = re.sub(pattern, r'+7(\2)\3-\4-\5', phone_clean)
    if ext:
        formatted += f' доб.{ext}'
    return formatted

for row in normalized:
    row[5] = format_phone(row[5])

contacts_dict = {}
for row in normalized:
    key = (row[0], row[1])
    if key not in contacts_dict:
        contacts_dict[key] = row
    else:
        existing = contacts_dict[key]
        for i in range(len(row)):
            if row[i] and not existing[i]:
                existing[i] = row[i]
        contacts_dict[key] = existing

result = [header] + list(contacts_dict.values())

with open("phonebook.csv", "w", encoding="utf-8", newline='') as f:
    datawriter = csv.writer(f, delimiter=',')
    datawriter.writerows(result)
print("Обработка завершена. Результат в phonebook.csv")

```

Лог:

```
2026-06-24 22:13:31.136861 – normalize_name(['Усольцев Олег Валентинович', '', '', 'ФНС', 'главный специалист – эксперт отдела взаимодействия с федеральными органами власти Управления налогообложения имущества и доходов физических лиц', '+7 (495) 913-04-78', 'opendata@nalog.ru']) -> ['Усольцев', 'Олег', 'Валентинович', 'ФНС', 'главный специалист – эксперт отдела взаимодействия с федеральными органами власти Управления налогообложения имущества и доходов физических лиц', '+7 (495) 913-04-78', 'opendata@nalog.ru']
2026-06-24 22:13:31.138614 – normalize_name(['Мартиняхин Виталий Геннадьевич', '', '', 'ФНС', '', '+74959130037', '']) -> ['Мартиняхин', 'Виталий', 'Геннадьевич', 'ФНС', '', '+74959130037', '']
2026-06-24 22:13:31.139259 – normalize_name(['Наркаев', 'Вячеслав Рифхатович', '', 'ФНС', '', '8 495-913-0168', '']) -> ['Наркаев', 'Вячеслав', 'Рифхатович', 'ФНС', '', '8 495-913-0168', '']
2026-06-24 22:13:31.139769 – normalize_name(['Мартиняхин', 'Виталий', 'Геннадьевич', 'ФНС', 'cоветник отдела Интернет проектов Управления информационных технологий', '', '']) -> ['Мартиняхин', 'Виталий', 'Геннадьевич', 'ФНС', 'cоветник отдела Интернет проектов Управления информационных технологий', '', '']
2026-06-24 22:13:31.140153 – normalize_name(['Лукина Ольга', '', 'Владимировна', 'Минфин', '', '+7 (495) 983-36-99 доб. 2926', 'Olga.Lukina@minfin.ru']) -> ['Лукина', 'Ольга', 'Владимировна', 'Минфин', '', '+7 (495) 983-36-99 доб. 2926', 'Olga.Lukina@minfin.ru']
2026-06-24 22:13:31.140390 – normalize_name(['Паньшин Алексей Владимирович', '', '', 'Минфин', '', '8(495)748-49-73', '1248@minfin.ru']) -> ['Паньшин', 'Алексей', 'Владимирович', 'Минфин', '', '8(495)748-49-73', '1248@minfin.ru']
2026-06-24 22:13:31.140726 – normalize_name(['Лагунцов Иван Алексеевич', '', '', 'Минфин', '', '+7 (495) 913-11-11 (доб. 0792)', '']) -> ['Лагунцов', 'Иван', 'Алексеевич', 'Минфин', '', '+7 (495) 913-11-11 (доб. 0792)', '']
2026-06-24 22:13:31.140997 – normalize_name(['Лагунцов Иван', '', '', '', '', '', 'Ivan.Laguntcov@minfin.ru']) -> ['Лагунцов', 'Иван', '', '', '', '', 'Ivan.Laguntcov@minfin.ru']
2026-06-24 22:13:31.141393 – normalize_name(['Лукина', 'Оксана', 'Владимировна', 'Минфин', '', '+7 (495) 983-36-99 доб. 2929', 'OLukina@minfin.ru']) -> ['Лукина', 'Оксана', 'Владимировна', 'Минфин', '', '+7 (495) 983-36-99 доб. 2929', 'OLukina@minfin.ru']
2026-06-24 22:13:31.143326 – format_phone('+7 (495) 913-04-78') -> '+7(495)913-04-78'
2026-06-24 22:13:31.143747 – format_phone('+74959130037') -> '+7(495)913-00-37'
2026-06-24 22:13:31.144065 – format_phone('8 495-913-0168') -> '+7(495)913-01-68'
2026-06-24 22:13:31.144292 – format_phone('') -> ''
2026-06-24 22:13:31.144539 – format_phone('+7 (495) 983-36-99 доб. 2926') -> '+7(495)983-36-99 доб.2926'
2026-06-24 22:13:31.144900 – format_phone('8(495)748-49-73') -> '+7(495)748-49-73'
2026-06-24 22:13:31.145246 – format_phone('+7 (495) 913-11-11 (доб. 0792)') -> '+7(495)913-11-11 доб.0792'
2026-06-24 22:13:31.145531 – format_phone('') -> ''
2026-06-24 22:13:31.145816 – format_phone('+7 (495) 983-36-99 доб. 2929') -> '+7(495)983-36-99 доб.2929'
```
